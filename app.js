const express = require("express");
const app = express();

// ...existing code...

// Elimina la configuración del motor de vistas
// app.set("views", path.join(__dirname, "views"));
// app.set("view engine", "ejs");

// ...existing code...